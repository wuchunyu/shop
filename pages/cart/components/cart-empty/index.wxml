<view class="flex align_center cart-empty">
  <t-image t-class="cart-img" src="{{imgUrl}}" />
  <view class="tip">{{tip}}</view>
  <view class="btn text_center" bind:tap="handleClick">{{btnText}}</view>
</view>