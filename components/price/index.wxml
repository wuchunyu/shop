<view class="price {{type}} wr-class">
	<view wx:if="{{type === 'delthrough'}}" class="line" style="height:{{utils.addUnit(lineThroughWidth)}};" />
	<view class="symbol symbol-class">¥</view>
	<view class="pprice">
		<view class="integer inline">{{price}}</view>
	</view>
</view>

